(sym_lib_table
  (version 7)
{% for lib in project.libs %} {# KiLib type #}
  (lib (name "{{lib.name}}")(type "KiCad")(uri "${KIPRJMOD}/{{lib.name}}.kicad_sym")(options "")(descr ""))
{% endfor %} {# project.libs #}
)
